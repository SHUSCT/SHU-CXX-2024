// "Include a header" means to copy the content of the header file to the source
// file.
#include "shucxx2024/HelloWorld.hpp"

// Main function is the ONLY entry point of a C++ program.
int main()
{
    // Call the function `sayHello` defined in the namespace `shucxx2024`.
    shucxx2024::sayHello();
    return 0;
}